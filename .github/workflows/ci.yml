name: CI

on:
  workflow_call:
  pull_request:
    branches: [main, beta]

jobs:
  qa:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        type: ['test', 'lint', 'build']
    name: Run ${{ matrix.type }}
    steps:
      - uses: actions/checkout@v5

      - name: Install tools with mise
        uses: jdx/mise-action@v3.3.0

      - name: Install dependencies
        run: pnpm install

      - run: pnpm ${{ matrix.type }}
