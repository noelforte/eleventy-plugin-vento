name: CI

on:
  push:
    branches: [renovate/**, dev]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: ./.github/actions/setup

      - name: Test
        run: pnpm test
